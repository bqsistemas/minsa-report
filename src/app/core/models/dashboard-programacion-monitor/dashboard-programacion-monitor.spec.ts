import { DashboardProgramacionMonitor } from './dashboard-programacion-monitor';

describe('DashboardProgramacionMonitor', () => {
  it('should create an instance', () => {
    expect(new DashboardProgramacionMonitor()).toBeTruthy();
  });
});
