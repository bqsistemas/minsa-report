import { RecursoInstrumento } from './recurso-instrumento';

describe('RecursoInstrumento', () => {
  it('should create an instance', () => {
    expect(new RecursoInstrumento()).toBeTruthy();
  });
});
